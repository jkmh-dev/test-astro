---
const { categoria } = Astro.props;
---

<a href={`/${categoria.nombre}`} class="block p-4 bg-white rounded-lg shadow-md hover:shadow-lg transition transform hover:scale-105">
  <div class="flex items-center justify-center text-center space-x-2">
    <span class="text-3xl">{categoria.icono}</span>
    <h2 class="text-xl font-semibold text-gray-900">{categoria.nombre.charAt(0).toUpperCase() + categoria.nombre.slice(1)}</h2>
  </div>
</a>

